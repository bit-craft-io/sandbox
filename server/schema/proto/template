syntax = "proto3";

package {{.PackageName}};

option go_package = "gen/proto;proto";
option php_namespace = "Proto\\{{.MessageName}}";

message Req{{.MessageName}} {
  // dummy
}

message Res{{.MessageName}} {
  // dummy
  bool is_success = 1;
  string message = 2;
}