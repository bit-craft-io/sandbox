apiVersion: v1
kind: Service
metadata:
  name: svc-director
spec:
  type: NodePort
  selector:
    app: director
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30091
